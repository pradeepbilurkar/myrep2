
import image_dehazer
HazeCorrectedImg, haze_map = image_dehazer.remove_haze(img1, showHazeTransmissionMap=False)
    return HazeCorrectedImg
import cv2
import numpy as np





    HazeImg = img1						# read input image -- (**must be a color image**)
    HazeCorrectedImg, haze_map = image_dehazer.remove_haze(img1, showHazeTransmissionMap=False)
    return HazeCorrectedImg
    #cv2.imshow('Original  and enhanced Image ', out);

